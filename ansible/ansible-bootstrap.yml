- name: Configure machine to be ansible workstation via public IP
  hosts: ansible
  sudo: yes
  vars:
    ansible_devs:
      - cwolfe
      - crochedutch
      - dip
  #pre_tasks:
  #  - include: roles/common/tasks/sssd.yml
  roles:
    # - common # TODO: only include when groups[loghost] intersect env is nonempty
    - ansible-bootstrap
