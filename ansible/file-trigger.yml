---
- name: Configure file-trigger server 
  hosts: file-trigger 
  user: ansible
  sudo: yes

  roles:
    - common
    - edware
    - { role: gluster-client, tags: ['gluster-client'] }
    - file-trigger 
