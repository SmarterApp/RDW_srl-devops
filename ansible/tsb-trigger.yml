---
- name: Configure tsb-trigger server 
  hosts: tsb-trigger 
  user: ansible
  sudo: yes

  roles:
    - common
    - edware
    - { role: aws-cli, tags: ['aws-cli'] }
    - { role: gluster-client, tags: ['gluster-client'] }
    - tsb-trigger 
