---
- name: Configure HPZ Web servers 
  hosts: hpz-web 
  user: ansible
  sudo: yes

  roles:
    - common
    - { role: aws-cli, tags: ['aws-cli'] }
    - { role: gluster-client, tags: ['gluster-client'] }
    - hpz-web 
