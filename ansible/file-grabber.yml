---
- name: Configure file-grabber server 
  hosts: file-grabber 
  user: ansible
  sudo: yes

  roles:
    - common
    - edware
    - { role: aws-cli, tags: ['aws-cli'] }
    - { role: gluster-client, tags: ['gluster-client'] }
    - file-grabber
